[project]
name = "quick-start"
version = "0.1.0"
description = "A quick start example using fed-rag."
readme = "README.md"
requires-python = ">=3.10,<4.0"
dependencies = [
  "fed-rag",
  "torch",
  "fire",
  "flwr-datasets>=0.5.0"
]

[tool.uv.sources]
fed-rag = {workspace = true}
