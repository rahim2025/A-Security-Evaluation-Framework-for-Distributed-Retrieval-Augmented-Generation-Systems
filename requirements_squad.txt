# ─────────────────────────────────────────────────────────────────────────────
# Requirements for ALL Reliable-dRAG attack scripts
#   • attack/Mia_attack/mia_attack.py      (MIA — real Docker)
#   • attack/Mia_attack/run_attack.py      (MIA runner)
#   • attack/selective_forward/run_attack.py (SFA — real Docker + SQuAD)
#   • run_sfa_squad.py / run_mia_squad.py / run_kb_squad.py  (standalone sims)
#
# Install:
#   pip install -r requirements_squad.txt
#
# Python 3.9 – 3.12 recommended (sentence-transformers wheels are available)
# ─────────────────────────────────────────────────────────────────────────────

# ── HTTP (real Docker endpoints) ─────────────────────────────────────────────
requests>=2.31.0

# ── SQuAD v1.1 dataset loader (SFA + standalone sims) ───────────────────────
datasets>=2.18.0
huggingface-hub>=0.22.0

# ── Embeddings — all-MiniLM-L6-v2 (MIA cosine similarity) ──────────────────
sentence-transformers>=2.7.0
torch>=2.0.0          # pulled in by sentence-transformers; listed explicitly
transformers>=4.40.0

# ── Numerical ────────────────────────────────────────────────────────────────
numpy>=1.24

# ── ML metrics — AUC-ROC, accuracy, precision, recall, F1 (MIA) ─────────────
scikit-learn>=1.3.0

# ── Graph topology (SQuAD standalone network sim) ───────────────────────────
networkx>=3.0

# ── Edit-distance / semantic similarity (KB extraction) ─────────────────────
rapidfuzz>=3.0.0

# ── Optional — pretty progress bars ─────────────────────────────────────────
tqdm>=4.66.0
